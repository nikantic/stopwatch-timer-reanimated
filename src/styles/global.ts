import { StyleSheet } from "react-native";

import { FONT_TYPES } from "../config/types";

const GLOBAL_STYLES = StyleSheet.create({
	text: {
		fontFamily: FONT_TYPES.MONO,
	},
});

export default GLOBAL_STYLES;
